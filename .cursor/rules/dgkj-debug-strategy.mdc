# 调试与排错策略：仅限云服务器

## 核心原则

**所有错误检查、调试、测试均在云服务器上进行，绝不在本地进行测试。**

## 具体要求

1. **不要在本地运行 `npm run dev` 或 `pnpm dev` 来测试功能**
2. **不要在本地启动前端服务来验证问题**
3. **排查问题时，通过以下方式：**
   - SSH 到云服务器 (`120.78.7.180`) 检查日志
   - 使用 GitHub Actions CI/CD 日志查看部署结果
   - 查看云服务器的 Nginx 日志、Node 服务日志

## 调试流程

### 云服务器日志路径

```bash
# Nginx 访问/错误日志
/var/log/nginx/

# Node 服务日志 (PM2)
/root/.pm2/logs/

# Docker 容器日志
docker logs <container_name>

# Java 服务日志
/root/dgkj-server/logs/
```

### GitHub Actions

- 前端部署触发 `deploy-cloud.yml` 或 `deploy-server.yml`
- 后端部署在云服务器上重新构建

## 为什么不在本地测试

1. Mock 数据与真实后端 API 行为不一致
2. 本地环境变量、网络配置与生产环境不同
3. 浪费时间在本地和云端之间来回切换
4. 云服务器是最终运行环境，本地没问题不代表云上没问题

## 何时可以本地运行

- 仅在添加新功能、修改页面布局时作为快速预览
- 任何功能验证和问题排查，**必须以云服务器为准**
