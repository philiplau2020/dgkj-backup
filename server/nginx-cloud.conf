# DGKJ 支付平台 - 云服务器 Nginx 配置
# 服务器: 120.78.7.180
# 域名: dghs.gddogootech.com

# ==================== 运营后台 ====================
server {
    listen 80;
    server_name dghs.gddogootech.com;

    # 前端静态文件
    root /www/dgkj/admin;
    index index.html;

    # Gzip 压缩
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml application/json application/javascript;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # API 请求代理到后端服务 (统一 /basic-api 前缀)
    location /basic-api/ {
        proxy_pass http://127.0.0.1:3000/basic-api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";

        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;

        # WebSocket 支持
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # Swagger 文档
    location /api-docs/ {
        proxy_pass http://127.0.0.1:3000/api-docs/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # 文件上传
    location /upload/ {
        proxy_pass http://127.0.0.1:3000/upload/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        client_max_body_size 100M;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # 日志
    access_log /var/log/nginx/dgkj-admin.access.log;
    error_log /var/log/nginx/dgkj-admin.error.log;
}

# ==================== HTTPS 配置 (可选) ====================
# 如果需要 HTTPS，可以使用以下配置
# server {
#     listen 443 ssl http2;
#     server_name dghs.gddogootech.com;
#
#     ssl_certificate /www/cert/dghs.gddogootech.com.pem;
#     ssl_certificate_key /www/cert/dghs.gddogootech.com.key;
#     ssl_session_timeout 5m;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE:ECDH:AES:HIGH:!NULL:!aNULL:!MD5:!ADH:!RC4;
#     ssl_prefer_server_ciphers on;
#
#     # ... 其余配置同上 ...
# }
#
# HTTP 重定向到 HTTPS
# server {
#     listen 80;
#     server_name dghs.gddogootech.com;
#     return 301 https://$server_name$request_uri;
# }
